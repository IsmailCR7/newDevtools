# devTools – проект для изучения инструментов разработки

## Quick Start

### Запуск приложения
1. Откройте проект в IntelliJ IDEA.
2. Перейдите в **Gradle Tool Window** (правая панель).
3. Раскройте **Tasks → application**.
4. Дважды щёлкните **run**.
5. Или используйте **Run Anything** (двойное `Ctrl`) и введите:
### Запуск тестов
1. В **Gradle Tool Window** раскройте **Tasks → verification**.
2. Дважды щёлкните **test**.
3. Или через **Run Anything**:

# CI/CD: GitHub Actions configured successfully
# DevTools Course

![Java CI](https://github.com/IsmailCR7/devtools/actions/workflows/ci.yml/badge.svg)

---
## Правила работы с ветками

Мы используем Git Flow с префиксом `DVT-` (DevTools).

### Основные ветки
- `master` / `main` — стабильная рабочая версия, всегда готовая к деплою
- `develop` — ветка для интеграции новых функций (если используется)

### Ветки разработки
Все новые задачи ведутся в feature-ветках по шаблону:

**Формат:** `тип/DVT-<номер_задачи>`

**Примеры:**
- `feature/DVT-3` — разработка новой функциональности для задачи DVT-3
- `feature/DVT-4` — разработка для задачи DVT-4



### Процесс работы
1. Создайте ветку от `master`: `git checkout -b feature/DVT-3`
2. Регулярно делайте коммиты с понятными сообщениями
3. После завершения работы создайте Pull Request (Merge Request)
4. После код-ревью ветка мержится в `master`
5. После создали от обновленного `master` ветку `git checkout -b feature/DVT-4`
6. Сделали коммит и создали PR.

## Git локальный цикл
1. Проверить статус
   git status
2. Добавить изменения
   git add <файлы>
3. Сделать коммит
   git commit -m "Сообщение"
4. git log --oneline
5.  Создать/переключить ветку
    git checkout -b feature/DVT-X
    git checkout master





## Описание пакета

Пакет `ru.mentee.power` содержит классы для отслеживания прогресса обучения студента (менти) в рамках спринтов по Java.

Основные классы:
- `ProgressDemo` – точка входа, демонстрация создания объекта прогресса.
- `MenteeProgress` – рекорд, представляющий данные о прогрессе менти.

---

## Поля рекорда `MenteeProgress`

| Поле       | Тип      | Описание                                      |
|------------|----------|-----------------------------------------------|
| `name`     | `String` | Имя менти                                     |
| `hours`    | `int`    | Количество часов, затраченных на спринт       |
| `finished` | `boolean`| Флаг завершения спринта                       |

---

## Методы рекорда

- `summary()` – возвращает форматированную строку с именем и часами.
- `readyForSprint()` – возвращает `true`, если менти готов к следующему спринту (≥3 часов).

---

## Ссылка на урок

Этот проект создан в рамках урока **«Инструменты разработки. Настройка окружения»**, часть спринта 2 курса **«Java с нуля до оффера»** (шаг 3 из 15).
🔗 **[Перейти к уроку](https://mentee-power.xl.ru/learn/MCIneBj4KkyH-GIRCspFvA/theory)**
---

## Структура проекта
devtools/
├── gradle/
├── src/
│ ├── main/java/ru/mentee/power/
│ │ ├── ProgressDemo.java
│ │ └── MenteeProgress.java
│ └── test/java/ru/mentee/power/
│ └── MenteeProgressTest.java
├── build.gradle
├── gradlew
├── gradlew.bat
└── README.md

---

## Зависимости

- **JUnit 5** – для модульного тестирования.
- **AssertJ** – для удобных assert-проверок в тестах.
## Feature DVT-4
Active development branch for DVT-4 task
## feature/DVT-5: Санитарный коммит очистки

### Хэш коммита очистки:
`772d40696f14e33c38b05096205df030cf103d48`

### Что сделано:
1. ✅ Обновлен `.gitignore` с правилами для мусорных файлов
2. ✅ Удалены .idea, build, .gradle из индекса Git
3. ✅ Добавлено правило «git status clean» в Runbook
4. ✅ `git check-ignore` корректно фильтрует .DS_Store, .idea/workspace.xml

### Команды для проверки:
bash
1. `git check-ignore -v .DS_Store`
2. `git check-ignore -v .idea/workspace.xml`
3. `git status`
4. `git diff --name-status HEAD~1..HEAD`

## Сценарий ручной проверки DVT-6

### Запуск приложения
1. Откройте Gradle Tool Window (View → Tool Windows → Gradle)
2. Выполните: devtools → Tasks → application → run
3. Ожидаемый вывод в Run Tool Window:
   Суммарно: пройдено 25 из 36 уроков, осталось 11 уроков

### Запуск тестов
1. Откройте Gradle Tool Window
2. Выполните: devtools → Tasks → verification → test
3. Ожидаемый вывод: BUILD SUCCESSFUL, все тесты зелёные

### Отладка через Debug
1. Установите breakpoint на строке цикла while в ProgressTracker.calculateProgress
2. Запустите Debug: кликните правой кнопкой на main → Debug 'ProgressTracker.main()'
3. Используйте Step Over (F8) для прохождения итераций
4. Проверьте Variables: counter, remainingHours должны изменяться корректно
5. Используйте Evaluate Expression (Alt+F8): вычислите remainingLessons * 2
6. Ожидаемый результат Evaluate: 14 (для completedLessons=5, totalLessons=12)

### Что делать при ошибках
- Если вывод некорректен: проверьте логику цикла через Debug
- Если тесты красные: откройте вывод теста, найдите AssertionError, скорректируйте метод
- Если Debug не останавливается: убедитесь, что breakpoint установлен (красный кружок)

## Кодстайл-гайд проекта devtools

Проект следует правилам Google Java Style Guide с адаптацией.
Автоматическая проверка: ./gradlew checkstyleMain

### 1. Именование методов: camelCase

До:    public void add_student(Student s) { }
После: public void addStudent(Student student) { }

Почему: Java Convention требует camelCase для методов.
Источник: https://google.github.io/styleguide/javaguide.html#s5.3-camel-case

### 2. Пробелы после if/for/while

До:    if(condition) {
После: if (condition) {

Почему: улучшает читаемость, отделяет ключевое слово от выражения.
Источник: Oracle Code Conventions — Whitespace

### 3. Длина строки: максимум 120 символов

До:    public List getStudentsFromSpecificCityWithVeryLongName...
После: public List getStudentsByCity(String city) {

Почему: длинные строки затрудняют чтение в редакторе и при code review.
Источник: https://google.github.io/styleguide/javaguide.html#s4.4-column-limit

### 4. Порядок импортов

До:    import java.util.List; import java.util.ArrayList; import java.io.File;
После: import java.io.File; import java.util.ArrayList; import java.util.List;

Почему: алфавитный порядок упрощает поиск импортов.
Источник: IntelliJ IDEA → Code → Optimize Imports

### 5. Фигурные скобки для if

До:    if (condition) doSomething();
После: if (condition) { doSomething(); }

Почему: скобки обязательны даже для однострочных блоков.
Источник: https://google.github.io/styleguide/javaguide.html#s4.1.1-braces-always-used



## Code Review Checklist


### Функциональность
- [ ] Код решает поставленную задачу полностью
- [ ] Обработаны граничные случаи (null, пустые данные, экстремальные значения)
- [ ] Обработка ошибок реализована корректно

### Тесты
- [ ] Добавлены тесты для нового функционала (или обновлены существующие)
- [ ] Все тесты проходят локально: `./gradlew test`
- [ ] Покрыты позитивные и негативные сценарии
- [ ] JaCoCo coverage ≥ 80% для нового кода

### Читаемость и стиль
- [ ] Имена переменных, методов и классов отражают назначение
- [ ] Нет дублирования кода (DRY principle)
- [ ] Checkstyle проходит без ошибок: `./gradlew checkstyleMain`
- [ ] Нет закомментированного кода или отладочного вывода (`System.out.println`)

### Документация
- [ ] README обновлён (если добавлена новая функциональность)
- [ ] Публичные методы имеют JavaDoc (если применимо)
- [ ] Примеры использования актуальны
- [ ] Runbook обновлён (если изменились команды запуска/проверки)

### Производительность и безопасность
- [ ] Нет очевидных проблем производительности
- [ ] Нет хардкода паролей, токенов или конфиденциальных данных

## Примеры Code Review комментариев

### Хорошие комментарии (конструктивные)

**Пример 1:**

**Проблема:** Метод `calculateDiscount` (строка 45) имеет 3 вложенных if-else и 40 строк.
**Почему это важно:** Сложная логика плохо тестируется и тяжело поддерживается.
**Предложение:** Вынести каждое условие в отдельный метод (например, `isEligibleForBonusDiscount()`)
и использовать паттерн Strategy для разных типов скидок.



**Пример 2:**

**Проблема:** Тест `testProcessOrder` (строка 78) проверяет только успешный сценарий.
**Почему это важно:** Не проверена обработка ошибок при недостаточном балансе.
**Предложение:** Добавить тест `testProcessOrder_InsufficientBalance_ThrowsException()`
с использованием `assertThatThrownBy()`.



### Плохие комментарии (неконструктивные)

**Пример 1:**

Этот код ужасен, полностью переписать.


**Почему плохо:** Нет конкретики (что именно плохо), нет предложения (как исправить),
токсичный тон (демотивирует автора).

**Пример 2:**

Здесь лучше использовать Stream API.


**Почему плохо:** Нет объяснения почему лучше, нет примера как переписать,
неясно какую проблему это решает.

## Результаты само-ревью DVT-9

### Найденные проблемы

#### 1. Отсутствие CI badge в README
**Файл:** README.md (начало файла)
**Проблема:** Нет бейджа статуса сборки GitHub Actions
**Почему важно:** CI badge показывает статус проекта сразу при открытии README —
это стандарт для open-source проектов и упрощает мониторинг состояния сборки.
**Исправление:** Добавить бейдж в начало README:

![CI](https://github.com/IsmailCR7/devtools/actions/workflows/ci.yml/badge.svg)



#### 2. Забыт неактуальный вывод
**Файл:** src/main/java/ru/mentee/power/devtools/ProgressDemo.java (строка 18)
**Проблема:** Оставлен `System.out.println("Текущая ветка разработки: feature/DVT-3")`
**Почему важно:** Отладочный вывод замусоривает логи production-приложения и создаёт впечатление небрежности.
**Исправление:** Исправлен вывод текста на актуальную ветку `System.out.println("Текущая ветка разработки: feature/DVT-9")`.

#### 3. Не правильное наименование проекта
**Файл:** src/main/java/ru/mentee/power/newDevtools/
**Проблема:** Проект должен называть "devtools", а фактически "newDevtools"
**Почему важно:** Создаёт путаницу и не проходит проверку SonarQube.
**Исправление:** Переименовал проект локально и на GitHub, закоммитил и запушил изменения.

## Информационный поиск — результаты DVT-11

### Запросы и источники

| № | Запрос | Операторы | Официальный источник | Альтернатива | Статус | Дата проверки |
|---|--|-----------|----------------------|--------------|--------|---------------|
| 1 | "lombok"  |site:search.maven.org "lombok"|https://central.sonatype.com/artifact/org.projectlombok/lombok/1.18.2?smo=true |https://central.sonatype.com/artifact/org.projectlombok/maven-lombok-plugin/0.9.3.1?smo=true |200 OK 03.02.26
| 2 |Java Stream API документация |site:docs.oracle.com "stream api" intitle:api java 25 |https://docs.oracle.com/en/middleware/standalone/coherence/15.1.1/develop-applications/using-repository-api.html#GUID-6FE2A09E-71FF-4D65-AB04-972B95BA87D6 |https://docs.oracle.com/en/database/other-databases/nosql-database/25.3/java-api/oracle/kv/pubsub/package-summary.html |200 OK 03.02.26
| 3 |горячие клавиши IntelliJ IDEA |site:www.jetbrains.com/help "keymap" "intellij idea" |https://www.jetbrains.com/help/idea/mastering-keyboard-shortcuts.html |https://www.jetbrains.com/help/idea/settings-keymap.html |200 OK  03.02.26

### AI-промпты и сравнение ассистентов

#### Промпт 1: План решения

**Задача:** «Нужно добавить валидацию email в класс Student»  
**Контекст:** Java 25, у меня есть класс Student с полем email (String), проект на Gradle
**Ограничения:** без использования регулярных выражений на данном этапе, простая проверка наличия @ и точки
**Ожидаемый результат:** план из 3-5 шагов, объяснение "почему именно так", НЕ пиши код — я реализую сам
**Критерии успеха:** валидация отклоняет пустые строки, строки без @, строки без точки после @

**AI-1 ([DeepSeek])**

Отличная задача. Вот план из пяти шагов, который следует принципам чистого кода и грамотного проектирования.

### План из 5 шагов

**Шаг 1: Определить метод валидации в классе `Student`**
*   **Что:** Создать приватный метод `isEmailValid(String email)`, который принимает строку и возвращает `boolean` (true — валиден, false — нет).
*   **Почему так:** Инкапсулируем логику проверки внутри класса, которому принадлежат данные. Приватный метод скрывает детали реализации от внешнего кода. Возврат `boolean` делает метод чистым и простым для тестирования.

**Шаг 2: Реализовать логику проверки внутри метода**
*   **Что:** Внутри метода последовательно проверить три условия:
    1.  Строка `email` не `null` и не пустая (`isBlank()` в Java 11+).
    2.  Строка содержит символ `'@'`. Использовать `indexOf('@') > 0`.
    3.  Символ `'.'` находится *после* символа `'@'`. Использовать `lastIndexOf('.') > email.indexOf('@')`.
*   **Почему так:**
    *   `indexOf('@') > 0` гарантирует, что `@` есть и перед ним есть хотя бы один символ (локальная часть адреса).
    *   `lastIndexOf('.') > email.indexOf('@')` гарантирует, что точка находится в доменной части (после `@`), а не в локальной. `lastIndexOf` учитывает домены с несколькими точками (например, `@example.co.uk`).
    *   Такой подход проще и читабельнее регулярных выражений на данном этапе и соответствует ограничениям.

**Шаг 3: Интегрировать валидацию в сеттер или конструктор**
*   **Что:** Найти место, где полю `email` присваивается значение. Это может быть:
    *   Конструктор класса `Student`, принимающий email.
    *   Сеттер (`setEmail`) для этого поля.
    *   В выбранном месте вызвать метод `isEmailValid` для входящей строки.
*   **Почему так:** Валидация должна выполняться в момент установки значения. Это гарантирует, что ни один объект `Student` не будет существовать с некорректным `email`. Это принцип сохранения целостности объекта.

**Шаг 4: Обработать случай невалидного email**
*   **Что:** Если метод `isEmailValid` возвращает `false`, выбросить проверяемое исключение, например, `IllegalArgumentException`.
*   **Почему так:** Использование проверяемого исключения (`IllegalArgumentException` — непроверяемое, что здесь тоже допустимо для аргумента, но идея та же) явно сигнализирует вызывающему коду об ошибке. Это надежнее, чем молчаливое игнорирование или возврат `false` из сеттера, и заставляет клиентский код обработать проблему.

**Шаг 5: Протестировать валидацию**
*   **Что:** Протестировать метод `isEmailValid` или конструктор/сеттер с разными сценариями:
    *   Корректные адреса (`user@example.com`, `name.surname@domain.co.uk`).
    *   Пустая строка и `null`.
    *   Адреса без `@` (`userexample.com`).
    *   Адреса, где `@` в начале (`@example.com`).
    *   Адреса без точки после `@` (`user@examplecom`).
    *   Адреса, где точка перед `@` (`user.@examplecom`).
*   **Почему так:** Тестирование граничных случаев — ключ к надежности. Это подтвердит, что валидация работает согласно критериям успеха и отклоняет все оговоренные некорректные форматы.

### Критерии успеха в этом плане
1.  **Пустые строки/`null`:** Отклоняются на первом шаге проверки (`isBlank()` или `== null`).
2.  **Строки без `@`:** Отклоняются условием `indexOf('@') > 0`.
3.  **Строки без точки после `@`:** Отклоняются условием `lastIndexOf('.') > email.indexOf('@')`.

Этот план обеспечивает четкую, поддерживаемую и тестируемую реализацию. Удачи в написании кода


**AI-2 ([Алиса])**

### План добавления валидации email в класс Student

1. **Добавить метод‑валидатор в класс Student**  
   *Почему:* логичное место для проверки — сам класс, владеющий данными. Метод будет инкапсулировать правила валидации, делая код самодостаточным и удобным для повторного использования.

2. **Определить сигнатуру метода**  
   *Почему:* метод должен принимать строку (email) и возвращать `boolean` (`true` — валидно, `false` — нет). Это упрощает интеграцию в логику приложения (например, для условных проверок). Название вроде `isValidEmail(String email)` сделает код самодокументируемым.

3. **Реализовать базовые проверки в методе**  
   *Почему:* по условиям нужны три критерия:
    - Непустота строки (`email != null && !email.isEmpty()`).
    - Наличие символа `@` (`email.contains("@")`).
    - Наличие точки *после* `@` (найти индекс `@`, затем проверить, есть ли `.` в подстроке после него).  
      Эти шаги покрывают требования без регулярных выражений.

4. **Интегрировать валидацию в сеттер поля email**  
   *Почему:* проверка при присваивании значения гарантирует, что объект `Student` всегда содержит корректный email. Если валидация не пройдена, можно:
    - выбросить исключение (`IllegalArgumentException`),
    - установить значение по умолчанию (например, `null`),
    - проигнорировать присваивание.  
      Выбор стратегии зависит от бизнес‑логики проекта.

5. **Добавить юнит‑тест для валидатора**  
   *Почему:* тесты подтвердят, что:
    - валидные email (например, `user@example.com`) проходят проверку,
    - невалидные (пустая строка, `user@com`, `userexample.com`) отклоняются.  
      Это снизит риск ошибок при будущих изменениях кода. Для Gradle подойдёт JUnit (добавляется в `dependencies` теста).

**Сравнение:**
Совпали в определении метода валидации в классе Student, ответ Алисы был гораздо короче и менее вариативным, в то время как DeepSeek ответил более развернуто и более понятно.



#### Промпт 2: Проверка кода

**Код для проверки:**

**Что проверить:**
1. Логические ошибки
2. Соответствие Code Conventions
3. Производительность

**AI-1 ([DeepSeek])** 

## Анализ кода

### 1. Логические ошибки
**Нет критических логических ошибок.** Код работает корректно. Однако есть потенциальные проблемы:

- **NullPointerException в цикле**: Если массив содержит `null` элементы, будет исключение при вызове `current.completedLessons()`. Нужна проверка внутри цикла.
- **Переполнение переменных**: При большом количестве mentee возможны переполнения `int` (2^31-1), но это маловероятно в данном контексте.

### 2. Code Conventions (Java)
**Нарушения:**

1. **Имена переменных**: `totalTotal` - неинформативное имя. Лучше `totalAllLessons` или `totalLessonsOverall`.
2. **Комментарии**: Избыточные комментарии, дублирующие код ("Увеличиваем счетчик - важно! без этого цикл будет бесконечным").
3. **Пустые строки**: Недостаточно группировки логических блоков.
4. **TODO комментарий**: Устаревший комментарий, который не был удалён после реализации.
5. **Дублирование кода**: Метод уже реализован, но остался шаблон TODO.

### 3. Производительность
**Оптимально:**
- Алгоритмическая сложность O(n) - оптимально для этой задачи.
- Используются примитивные типы, что эффективно по памяти.

**Возможные улучшения:**
- Использовать `for-each` вместо `while` для читаемости.
- Добавить проверку на переполнение для надёжности.

---
## Ключевые улучшения:

1. **Безопасность**: Добавлена проверка на `null` элементы внутри массива
2. **Защита от переполнения**: Добавлен метод проверки переполнения при сложении
3. **Читаемость**:
    - Заменён `while` на `for-each`
    - Улучшены имена переменных
    - Удалены избыточные комментарии
    - Удалён устаревший TODO
4. **Обработка ошибок**: Возврат сообщения при переполнении
5. **Стиль кода**: Улучшено форматирование и группировка кода


**AI-2 ([Алиса])**

Проведу полный анализ кода по ключевым критериям: логические ошибки, соответствие Code Conventions (Java), производительность.

## 1. Логические ошибки

**Критических логических ошибок нет.** Алгоритм корректно:
* обрабатывает крайние случаи (`null`/`empty`);
* накапливает значения через цикл `while`;
* вычисляет остаток как разницу;
* формирует корректный вывод.

**Потенциальные проблемы:**
* Нет проверки на `null` внутри массива `mentees` (если какой‑то элемент `null`, будет `NullPointerException`).
* Нет валидации входных данных в `Mentee` (например, `completedLessons > totalLessons`).

## 2. Соответствие Code Conventions (Java)

**Нарушения:**
1. **Комментарии к методу**  
   Используются `*` вместо `*`, что нарушает стандарт Javadoc:
   ```java
   /**
    * Вычисляет...
    * @param mentees...
    */
   ```
   → Должно быть:
   ```java
   /**
    * Вычисляет...
    * @param mentees...
    */
   ```

2. **Именование переменных**  
   `totalCompleted`, `totalTotal`, `totalRemaining` — избыточное `total`. Лучше:
    * `completedLessons`
    * `totalLessons`
    * `remainingLessons`

3. **Формат строки в `String.format`**  
   Нет пробелов после запятых в выводе. Лучше:

   "Суммарно: пройдено %d из %d уроков, осталось %d уроков"
   ```
   →

   "Суммарно: пройдено %d из %d уроков, осталось %d уроков"
   ```

4. **Отступы и форматирование**  
   В некоторых IDE может быть нарушено выравнивание (зависит от настроек).

## 3. Производительность

**Текущее решение:**
* Цикл `while` с ручным инкрементом — корректно, но менее читаемо, чем `for`.
* Временная сложность: **O(n)** — оптимально.
* Память: **O(1)** — оптимально.

**Оптимизации:**
1. Заменить `while` на `for` для лучшей читаемости

2. Использовать `Objects.requireNonNull` для проверки `mentees`:
 

3. Добавить проверку на `null` внутри цикла Mentee


## 4. Дополнительные рекомендации

1. **Обработка исключений**  
   Добавить `try-catch` для `NullPointerException`, если возможны `null`-элементы.

2. **Юнит-тесты**  
   Необходимо покрыть кейсы:
    * `null`-массив;
    * пустой массив;
    * массив с `null`-элементами;
    * корректные данные.

3. **Документация**  
   Добавить Javadoc для класса и метода `main`.

4. **Константы**  
   Вынести строку формата в константу:
   ```java
   private static final String PROGRESS_FORMAT = 
       "Суммарно: пройдено %d из %d уроков, осталось %d уроков";
   ```


**Вывод:** код работоспособен, но требует доработки для соответствия стандартам Java и повышения устойчивости.


**Сравнение:**
Интересно что ассистенты практически ни в чем не совпали, так же интересный момент, что Алиаса начала "выдумывал" ошибки в синтаксисе и в кодстайле, предлагать исправить на то же самое, что и так написано в коде, DeepSeek показал себя лучше и вариативнее, но иногда для новой мысли с другой стороны можно протестить, что предлагает другой ассистент.